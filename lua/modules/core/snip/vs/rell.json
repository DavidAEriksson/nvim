{
  "query + name + from + where + select": {
    "body": "query ${1:name} () = ${2:from} @ { ${3:where} } ( ${4:select} );",
    "prefix": [
      "query",
      "name",
      "from",
      "where",
      "select"
    ]
  }
}
